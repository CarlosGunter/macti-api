Hola. buenas tardes, espero estén muy bien.

Con relación a los avances que hice fueron los siguiente:
1.- cree el archivo email_service que es el que manda el correo con el token
cambien los valores del SMTP para que pueda mandar correo. 
según yo sería algo así
SMTP_HOST = 'smtp.gmail.com'
SMTP_PORT = 587  # STARTTLS
SMTP_USER = 'tu_correo@gmail.com'
SMTP_PASS = 'pass con la que entras al correo'
FROM_ADDRESS = 'tu_correo@gmail.com'
FROM_NAME = 'MActi proto'

2.-En rutas agrege el endpoint para crear el correo 
@router.post("/validate-email", summary="Enviar correo de validación")
async def validate_email(body_info: EmailValidationSchema):
    result = EmailService.send_validation_email(to_email=body_info.email)
    return result

3.- en schema agrege el nombre la clase que estoy usando
class EmailValidationSchema(BaseModel):
    email: EmailStr

/*******
Me falto agregarlo a bd porque revisando la bd, con inspect_bd que hice, solo hay dos tablas y no sé sobre la estrucutra que tienen plantada dónde se captura o como validaran el tocken. Quedo pendiete
